import { RegisterForm } from "@/components/auth/register-form";

export const metadata = {
  title: "Register | Guyana Loan Tracker",
  description: "Create your loan tracking account",
};

export default function RegisterPage() {
  return <RegisterForm />;
}
